id: amz-lc-last-stone-weight-ii
title: Last Stone Weight II (edited)
type: dsa
tags:
  - amazon
  - discord
topics:
  - arrays
  - dp
difficulty: medium
status: extracted
confidence: medium
evidence: discord-title
prompt: |
  You are given an array of integers `stones` where `stones[i]` is the weight of the `i^th` stone.
  
  We are playing a game with the stones. On each turn, we choose any two stones and smash them together. Suppose the stones have weights `x` and `y` with `x <= y`. The result of this smash is:
  
  - If `x == y`, both stones are destroyed, and
  
  - If `x != y`, the stone of weight `x` is destroyed, and the stone of weight `y` has new weight `y - x`.
  
  At the end of the game, there is at most one stone left.
  
  Return the smallest possible weight of the left stone. If there are no stones left, return `0`.
examples:
  - input: 'stones = [2,7,4,1,8,1]'
    output: '1'
    explanation: 'We can combine 2 and 4 to get 2, so the array converts to [2,7,1,8,1] then,'
  - input: 'stones = [31,26,33,21,40]'
    output: '5'
constraints: |
  - `1 <= stones.length <= 30`
  - `1 <= stones[i] <= 100`
starter: |
  from typing import List
  
  class Solution:
      def lastStoneWeightII(self, stones: List[int]) -> int:
          pass
  
  # print(Solution().lastStoneWeightII(...))  # e.g. stones = [2,7,4,1,8,1]
leetcode:
  slug: last-stone-weight-ii
  id: 1049
  title: Last Stone Weight II
  relationship: exact
occurrences:
  - company: amazon
    contributor: online
    evidence: discord-title
sources:
  - kind: discord
    path: extras/zonline discord/LC qs.txt
    role: prompt
notes: Official LeetCode statement filled from public problem data. Discord row was title-only.
updated_at: 2026-08-31
